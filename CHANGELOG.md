# 版本历史（用法示例）

本仓库不在此维护逐条变更记录；请直接用 git 命令查看历史。

## 查看最近提交
```bash
git log --oneline -n 30
```

## 按文件查看历史
```bash
git log --oneline -- docs/BOOKS_README.md
```

## 查看某个提交的变更
```bash
git show <commit_sha>
```
